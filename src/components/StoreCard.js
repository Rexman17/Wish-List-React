import React from 'react';


const StoreCard = (props) => {
  console.log("I AM A StoreCard");

  return (
    <div>ALI</div>
  )
}

export default StoreCard;

// {props.clicked ? <div>{props.storeName}</div> : null}
